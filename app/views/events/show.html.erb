  <div class="event">
    <h1><%= @event.name %></h1>
    <p>Date: <%= @event.date %></p>
    <%= link_to 'Edit', edit_event_path(@event) %>
    <%= link_to 'Delete', event_path(@event), method: :delete, data: {confirm: "Are yu sure you want to delete the #{@event.name} event?"} %>
  </div>
